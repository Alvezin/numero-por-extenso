// desafio: GERAR UM NÚMERO POR EXTENSO 
import { getArrayEl,getLessHundred,getLessThousand,
    joinNumber, lastArrayItem, splitNum, arrayOfNumber, casas,
    hundredTohundred, imutaveis, tenMutiples
} from   './functions'
/*
function numeroExtenso (num:number) {
    splitNum(num)
    if(num <= 19 ){
        return getArrayEl(imutaveis, num)
    }
    if(num > 19 && num <= 100) {
        return getTwentytoHundred(0, 1)
    }
    if( num >= 101 && num < 1000) {
        return getMoreThanHundred(0, 1, 2)
    }
    if( num >= 1000 && num < 999999 ) {
    }
}
console.log(numeroExtenso(999))
*/

arrayOfNumber.push(1,5,0)

console.log(getLessThousand(0,1,2))